<%provide(:title, @category.title)%>
<div class="row">
    <aside class="span">
        <section>
            <h1>
                <%= @category.title %>
            </h1>
            <p><%= @category.description %></p>
        </section>
    </aside>
    <div>
        <%= link_to "Add Word", new_admin_category_word_path(@category.id) %>
    </div>
</div>

        
 