<%provide(:title, @category.title)%>
<div class="row">
    <aside class="span">
        <section>
            <h1>
                <%= @category.title %>
            </h1>
            <p><%= @category.description %></p>
        </section>
    </aside>
</div>

        
 